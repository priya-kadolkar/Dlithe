//Datatypes

let itemName = "TV";   // String using double quotes
let itemType = 'Home appliances';   // String using single quotes
console.log(typeof(itemName))
console.log(typeof(itemType))

let costId=1; //Number datatype without decimals
let costId1=2.2//Number datatype decimals
console.log(typeof(costId) )
console.log(typeof(costId1))

let item1=false; //Boolean datatype
let item2=true; //Boolean datatype

console.log( typeof(item1))
console.log(typeof(item2))

let employees={
    name:"Priya",
    age:26
}
console.log(typeof(employees))

let b;
console.log(typeof(b))
